################### Inodebeat Configuration Example #########################

############################# Inodebeat ######################################

inodebeat:
  # Defines how often an event is sent to the output
  period: 1s
  directories: ["/"]
